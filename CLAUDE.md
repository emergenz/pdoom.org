# pdoom.org

## Writing style

- **Never use em dashes (the `—` character) anywhere in this repo**: not in docs,
  marketing and research pages, README/markdown, code comments, or UI strings.
  They read as AI-generated. Use a colon, comma, parentheses, or a separate
  sentence instead; for a title or label separator use a middle dot (`·`).
