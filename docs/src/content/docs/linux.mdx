---
title: Linux (experimental)
description: "crowd-cast on Linux: compatibility matrix across X11/Wayland and desktop environments, install, requirements and known limitations."
---

import { Aside, Steps } from '@astrojs/starlight/components';

<Aside type="caution" title="Experimental">
  Linux support is **experimental** and evolving. What works depends heavily on
  your **display server** (X11 vs Wayland) and **compositor / desktop
  environment**. Read the [compatibility matrix](#compatibility-matrix) before
  relying on it, and see [known limitations](#known-limitations) for what isn't
  validated yet.
</Aside>

crowd-cast follows a strict **fail-closed** design: it never silently degrades.
If a prerequisite for correct capture isn't met in your environment, recording
is **blocked** with a clear reason rather than recording something incomplete or
wrong.

## Compatibility matrix

For each environment, this is what's currently implemented:

<div class="cap-matrix">

| Environment | App-window video | Follow-focus | System tray | Input |
| --- | --- | --- | --- | --- |
| **X11** (any WM) | <span class="dot ember">●</span> per-window | <span class="dot ember">●</span> yes | <span class="dot brass">●</span> SNI host | <span class="dot ember">●</span> rdev |
| **Wayland · wlroots** | <span class="dot dim">○</span> full-screen | <span class="dot ember">●</span> yes | <span class="dot brass">●</span> SNI host | <span class="dot ember">●</span> evdev |
| **Wayland · GNOME** | <span class="dot ember">●</span> per-app (portal) | <span class="dot brass">●</span> extension | <span class="dot brass">●</span> AppIndicator | <span class="dot ember">●</span> evdev |
| **Wayland · KDE Plasma** | <span class="dot ember">●</span> per-app (portal) | <span class="dot dim">○</span> blocked | <span class="dot ember">●</span> native | <span class="dot ember">●</span> evdev |
| **Wayland · other** | <span class="dot brass">●</span> per-app, if the portal offers it | <span class="dot dim">○</span> blocked | <span class="dot brass">●</span> SNI host | <span class="dot ember">●</span> evdev |

</div>

**Legend:** <span class="dot ember">●</span> works · <span class="dot brass">●</span> works, with a dependency · <span class="dot dim">○</span> not available.

Notes:

- **wlroots** covers Sway, Hyprland, river, Wayfire and labwc.
- **Per-app video on Wayland** goes through the desktop portal: per-app where the portal offers a window source (GNOME, KDE), and full-screen where it only offers the whole output (wlroots).
- **Blocked** means recording is gated off until a focus provider exists; the agent fails closed rather than recording incorrectly.
- **System tray** needs a StatusNotifier host: KDE provides one natively, GNOME via the AppIndicator extension, and most bars (waybar, XFCE, MATE, LXQt) support it fully. **On Sway, swaybar shows the icon but does not open the menu, so run waybar instead.**
- **evdev** input capture needs `input`-group membership; X11 uses `rdev` and needs no group change.
- **GNOME** follow-focus needs the bundled [extension](#gnome) and a relogin.
- **Not yet validated:** X11 per-app capture and the GNOME focus path are implemented and unit-tested, but not verified end-to-end on real hardware.

The short version: **X11** is the most complete setup, with both per-window
capture and input gating. On **wlroots compositors (sway, Hyprland)** input
gating works but the portal only allows full-screen capture. **GNOME** captures
per-app and needs our shell extension for input gating. **KDE** captures per-app
but has no input-gating provider yet, so recording is gated off there.

## Requirements

- **x86_64** Linux. (Other architectures aren't packaged.)
- `tar` and `zstd` available (used to unpack the bundled capture components).
- On **Wayland**, membership in the **`input`** group (for `evdev` input
  capture). The installer sets this up for you.
- Screen capture (libobs) is **bundled**, so you don't need to install OBS.

## Installing

See [Install → Linux](/docs/crowd-cast/install/) for the step-by-step
instructions. In short, it's a user-space install (into `~/.local`) run from a
checkout with `packaging/linux/install.sh --local`, plus a single `sudo` step to
add you to the `input` group. The script lays out:

```text
~/.local/bin/crowd-cast-agent                   # the agent
~/.local/share/crowd-cast/obs/<abi>/...          # bundled capture components
~/.local/share/applications/crowd-cast.desktop   # menu entry
~/.config/autostart/crowd-cast.desktop           # start at login
```

## Display servers & per-app capture

- **X11**: crowd-cast can capture a **single window** (per-app) using
  XComposite, following the focused window. This needs a pure X11 session with
  `_NET_ACTIVE_WINDOW` and the Composite extension (standard on virtually all
  X11 desktops).
- **Wayland**: per-window capture goes through the **xdg-desktop-portal
  ScreenCast** API. Where the portal offers a window source (GNOME, KDE),
  crowd-cast captures the selected apps per-window and prompts once per app to
  share its window (the choice is remembered afterwards). Where the portal only
  offers the whole output (**wlroots compositors such as sway and Hyprland**),
  the per-app picker is disabled and only full-screen capture is available, so it
  is your choice whether to record there. Either way,
  follow-focus is used to **gate input** (stop logging keystrokes and mouse when
  a non-selected app is focused).

## Which monitor

- **X11**: per-app capture follows the window itself, on whichever monitor it is
  on. Full-screen capture (where per-app isn't available) records the **primary
  screen**.
- **Wayland**: you choose the source through the desktop portal. For per-app
  capture you pick each app's window once and the choice is remembered. For
  full-screen capture (wlroots compositors such as sway and Hyprland) the portal
  asks you to pick the **output to share**, and because that choice isn't
  persisted, it is asked again **each time crowd-cast starts**.

## Follow-focus by environment

"Follow-focus" means knowing which app is in front, so input is only logged for
your selected apps. The mechanism differs per environment, and there is **no
fallback**: if no reliable provider exists for your environment, recording is
blocked:

- **X11**: reads the active window directly. Works.
- **wlroots** (Sway, Hyprland, river, Wayfire, labwc): uses the
  `wlr-foreign-toplevel` protocol. Validated on Sway.
- **GNOME**: see [below](#gnome); requires our extension.
- **KDE Plasma and other Wayland compositors**: not implemented yet, so
  recording is gated off.

### GNOME

GNOME (Wayland) provides **no app-callable focus API**, so crowd-cast ships a
small, read-only **GNOME Shell extension** that reports the focused window over
a private interface. Because Wayland can't hot-load a new extension, you'll need
to **install it and log out/in once** to activate it. Until it's live, recording
on GNOME is gated off. This path currently compiles but hasn't been validated on
a live GNOME session yet.

## Input capture

On **Wayland**, input is captured via **`evdev`**, which requires your user to be
in the **`input`** group. The installer adds you automatically; to do it by hand:

```bash
sudo usermod -aG input "$USER"
# then log out and back in
```

If you're not in the group, crowd-cast can't read input devices and will tell you
so (e.g. *"No input devices found. Make sure you are in the 'input' group."*). On
**X11**, input is captured via `rdev` and no group change is needed.

## System tray

The Linux tray uses the freedesktop **StatusNotifierItem (SNI)** protocol. What
appears depends on your status bar:

- **Icon and menu both work:** KDE Plasma (native), **waybar**, GNOME with the
  **AppIndicator** extension, XFCE, MATE, Cinnamon, LXQt.
- **Icon only:** **swaybar** (Sway's default bar) shows the icon but does not open
  the menu, so you cannot control crowd-cast from it.
- **Nothing at all:** with no SNI host (vanilla GNOME without the AppIndicator
  extension, or a bare tiling WM), the icon does not appear.

<Aside type="caution" title="Sway users">
  Under **Sway**, the default **swaybar** shows the crowd-cast icon but does not
  open its menu. Run **waybar** (with its `tray` module) instead and both the icon
  and the menu work normally. Either way the agent keeps running and recording;
  the tray is only for control.
</Aside>

## Autostart & updates

- **Start at login** is an XDG autostart entry at
  `~/.config/autostart/crowd-cast.desktop`.
- **Updates** are delivered as signed (Ed25519) releases that the agent verifies
  and installs in place, then relaunches, mirroring the automatic in-app update
  on macOS.

## Configuration

Your config lives at `~/.config/agent/config.toml`. The options
are the same across platforms. See [Configuration](/docs/crowd-cast/configuration/).

## Known limitations

- **Per-app capture on Wayland depends on the desktop portal.** GNOME and KDE
  offer a window source, so capture is per-app there; wlroots compositors (sway,
  Hyprland) only allow full-screen capture. X11 captures per-window via XComposite.
- **X11 per-app capture is unit-tested but not yet live-validated.**
- **GNOME follow-focus** compiles but hasn't been exercised on a real GNOME
  session, and requires the bundled extension + a relogin.
- **KDE Plasma and other Wayland compositors** have no focus provider yet, so
  recording is gated off there.
- **Tray control depends on your status bar.** Under swaybar the icon appears but
  its menu does not open (use waybar); with no SNI host at all (vanilla GNOME
  without AppIndicator) the icon does not appear. See [System tray](#system-tray).

These and other platform quirks are tracked on the
[Known issues](/docs/crowd-cast/known-issues/) page.
